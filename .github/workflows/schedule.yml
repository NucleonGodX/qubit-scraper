name: Schedule Scraper

on:
  push:   
    branches:
      - main
  schedule: 
    - cron: "0 */3 * * *"

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Scraper 
        run: |
          curl -X GET https://qubit-scraper-amazing-systummm-ewjqy.onrender.com/run_scraper --silent --output /dev/null --max-time 5